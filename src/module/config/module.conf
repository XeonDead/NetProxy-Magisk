# NetProxy 模块设置
# 开机自动启动服务 (1=启用, 0=禁用)
AUTO_START=1
# 出站模式 (rule=规则分流, global=全局代理, direct=全局直连)
OUTBOUND_MODE=rule
# 节点选择模式 (manual=手动选择, urltest=动态测速)
SELECTOR_MODE=urltest
# GMS 修复 (1=启用, 0=禁用)
GMS_FIX=0
# 当前使用的 sing-box 出站配置文件路径
CURRENT_CONFIG=""
